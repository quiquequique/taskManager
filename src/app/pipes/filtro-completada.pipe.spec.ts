import { FiltroCompletadaPipe } from './filtro-completada.pipe';

describe('FiltroCompletadaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompletadaPipe();
    expect(pipe).toBeTruthy();
  });
});
